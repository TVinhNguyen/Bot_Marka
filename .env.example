# Environment switch: dry_run | demo | live (only dry_run is implemented in this slice)
ENV=dry_run
DRY_RUN=true

# MT5 placeholders (not used in dry_run skeleton; real values needed for issue #3)
MT5_LOGIN=
MT5_SERVER=
MT5_PASSWORD=
