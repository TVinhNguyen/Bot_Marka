# Use Closed Bars And Walk-Forward Validation

The system uses closed bars only and requires walk-forward out-of-sample validation before demo or live promotion. This prevents running-bar drift and look-ahead bias, even though it delays signals and makes research slower than simpler random-split backtests.

