# Keep Agents Out Of Order Execution

The system deliberately keeps the agent layer out of order submission and live risk mutation. Agents may orchestrate, format, and narrate deterministic module outputs, but only the executor may submit broker orders after a risk decision approves them; this trades autonomy for auditability and account safety.

